package chapter01.ExcutableJavaFiles;

public class JavaTest {
	/*
	 * One can compile and execute java 
	 * programs using the command-line programs
	 * of JAVAC and JAVA. 
	 * 
	 * javac[Options][Source Files]
	 * Options: -version and -help
	 * 
	 * java[options] class[args]
	 * 
	 * Options: -version 
	 * class[args]: -"call to class of the source file"
	 * 
	 * */
}
